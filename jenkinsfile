pipeline{
    agent any
    stages {
        stage('build') {
            steps {
                sh "npm_install"
            }
        }
        stage('tests') {
            sh "npm test"
        }
    }
}