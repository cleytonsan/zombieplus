pipeline{
    agent {
        docker {image "node"}
    }
    stages {
        stage('build') {
            steps {
                sh "npm_install"
            }
        }
        stage('tests') {
            sh "npm run test:ci"
        }
    }
}